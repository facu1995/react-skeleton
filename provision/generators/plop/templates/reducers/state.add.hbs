import { {{properCase reducerName}}State } from "../interfaces/interfaces";
import { {{properCase reducerName}}Action } from '../actions/actions';

export const {{lowerCase reducerName}}Reducer = (state: {{properCase reducerName}}State, action: {{properCase reducerName}}Action): {{properCase reducerName}}State => {
    switch (action.type) {
        case 'increaseBy':
            return { {{lowerCase reducerName}}: state.{{lowerCase reducerName}} + action.payload.value }
        case 'reset':
            return { {{lowerCase reducerName}}: 0 }
        default:
            return state;
    }
}